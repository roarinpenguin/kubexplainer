# Kubernetes YAML Explainer - Environment Variables

# Server Port
PORT=8080

# Database URL (SQLite by default)
DATABASE_URL=sqlite+aiosqlite:///./data/app.db

# Encryption Key for API keys (CHANGE THIS IN PRODUCTION!)
ENCRYPTION_KEY=kubernetes-yaml-explainer-change-me-in-production

# Optional: PostgreSQL Database (uncomment to use instead of SQLite)
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/k8s_explainer

# Optional: CORS Origins (comma-separated list)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
